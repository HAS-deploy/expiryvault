name: ExpiryVault
options:
  bundleIdPrefix: com.expiryvault
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: "NH2XFPC9KN"
    CODE_SIGN_STYLE: Automatic

packages:
  PostHog:
    url: https://github.com/PostHog/posthog-ios
    from: "3.15.0"

targets:
  ExpiryVault:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: ExpiryVault
        excludes:
          - "**/.DS_Store"
    resources:
      - ExpiryVault/Resources/Assets.xcassets
      - ExpiryVault/Resources/PrivacyInfo.xcprivacy
      - ExpiryVault/Resources/Configuration.storekit
    dependencies:
      - package: PostHog
        product: PostHog
    info:
      path: ExpiryVault/Resources/Info.plist
      properties:
        CFBundleDisplayName: ExpiryVault
        CFBundleShortVersionString: "1.0.0"
        CFBundleVersion: "2"
        LSRequiresIPhoneOS: true
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        ITSAppUsesNonExemptEncryption: false
        PostHogKey: "phc_zsZ6KoWfgoUyVnJEjKi5KC6CuS66xnyQgradTRmqc52s"
        PostHogHost: "https://us.i.posthog.com"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.expiryvault
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: NO
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: YES
        CURRENT_PROJECT_VERSION: "2"
        MARKETING_VERSION: "1.0.0"
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_PREVIEWS: YES
        SWIFT_EMIT_LOC_STRINGS: YES

  ExpiryVaultTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: ExpiryVaultTests
    dependencies:
      - target: ExpiryVault
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.expiryvault.tests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  ExpiryVault:
    build:
      targets:
        ExpiryVault: all
        ExpiryVaultTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ExpiryVaultTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
